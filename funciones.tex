% !TEX root=main.tex
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% FUNCIONES
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{contentbox}{label=Funciones Nativas}
    %Además de \mintinline{python3}|print| e \mintinline{python3}|input|, las siguientes funciones, entre otras, están siempre disponibles:
    Estas funciones, entre otras, están siempre disponibles:
    
    \begin{tabular}{C|p{0.5\linewidth}}
        \mintinline{python3}|abs(x)| & Valor absoluto de \texttt{x}. \\
        \mintinline{python3}|help(x)| & Muestra ayuda sobre \texttt{x}. \\
        \mintinline{python3}|max(seq)| & Retorna el máximo de \texttt{seq}. \\
        \mintinline{python3}|min(seq)| & Retorna el mínimo de \texttt{seq}.\\
        \mintinline{python3}|range(stop)| & \multirow{2}{=}{Genera una secuencia de valores enteros.} \\
        \mintinline{python3}|range(i, f, s)| & \\
        \mintinline{python3}|round(x)| & \multirow{2}{=}{Redondea \texttt{x} a entero o a \texttt{n} decimales.} \\
        \mintinline{python3}|round(x, n)| & \\
        \mintinline{python3}|sorted(seq)| & Retorna la secuencia \texttt{seq} ordenada \\
        \mintinline{python3}|type(o)| & Retorna el tipo de \texttt{o} \\
        \mintinline{python3}|isinstance(o, c)| & Verifica si \texttt{o} es del tipo \texttt{c} \\
    \end{tabular}
    
    El nombre de cada tipo de dato es la función para cambiar a dicho tipo de dato, p.e.: \mintinline{python3}|x = int(y)| o \mintinline{python3}|l = list(s)| (si \texttt{s} es un conjunto o secuencia, como un string).
\end{contentbox}


\begin{contentbox}{label=Funciones Importadas}
    Un módulo es un archivo que contiene definiciones y declaraciones en Python.
    
    Existen módulos estándar (siempre disponibles para importar), definidos por el programador y externos (instalados en el sistema).
    
    Para importar, utilizamos:
    \begin{minted}{python3}
import module
# Uso de, por ejemplo, func1:
module.func1(params)
# Alternativamente, podemos dar un 
# alias
import module as bla
bla.func1(params)
    \end{minted}
    
    En casos especiales, podemos importar solo las funciones de interés utilizando:
    \begin{center}
        \mintinline{python3}|from module import func1, func2, func3|
    \end{center}
    De modo que se pueda utilizar \verb|func1|, \verb|func2| y \verb|func3| como si fuesen nativas.
\end{contentbox}


\begin{contentbox}{label=Módulos Estándar}
    \small
    \begin{tabular}{C|p{0.65\linewidth}}
        \mintinline{python3}|math| & Funciones matemáticas. \\
        \mintinline{python3}|sys| & Funciones y parámetros del intérprete. \\
        \mintinline{python3}|op| & Interfaces de sistema operativo. \\
        \mintinline{python3}|op.path| & Manipulaciones de rutas de archivo. \\
        \mintinline{python3}|random| & Generación de números pseudo aleatorios. \\
        \mintinline{python3}|datetime| & Tipos básicos de fecha y hora. \\
        \mintinline{python3}|copy| & Operaciones de copia superficial y profunda. \\
        \mintinline{python3}|time| & Operaciones de tiempo. \\
        \mintinline{python3}|itertools| & Iteradores especiales.
    \end{tabular}
\end{contentbox}


\begin{contentbox}{label=Definición de Funciones Propias}
    La estructura básica de definición de funciones es:
\begin{minted}{python3}
def func_name(arg1, arg2, arg3=valor):
    definiciones_y_operaciones_locales
    return resultado
\end{minted}

    \begin{itemize}
        \item Dondequiera que se encuentre \mintinline{python3}|return|, se finaliza la función y se entrega ese resultado. Puede haber varios, según si los resultados son condicionados.
        \item Los parámetros son tantos como se requieran. Si no se requieren, los paréntesis van vacíos.
        \item Los parámetros opcionales se escriben al final, dándoles su valor por defecto (como en \verb|arg3|).%, también separándolos por coma.
    \end{itemize}

    Una vez definida, se llama como una función nativa normal:
\begin{minted}{python3}
# Solo parámetros obligatorios
r = func_name(x, y)
# Con el parámetro opcional
r = func_name(x, y, z)
# Dando nombre del parámetro opcional
r = func_name(x, y, arg3=z)
\end{minted}
\end{contentbox}

\begin{contentbox}{label=Recursión}
    Python solo requiere el nombre de la función para poder utilizarla, por lo que podemos construir una función con base en sí misma:
\begin{minted}{python3}
def func(x):
    if condición(x):
        new_x = operaciones(x)
        return func(new_x)
    else:
        return otras_operaciones(x)
\end{minted}
\end{contentbox}

\begin{contentbox}{label=Comentarios y Documentación}
    Las líneas comenzadas por \mintinline{python3}|#| son comentarios e ignorados por el intérprete. Sirven para documentar el código para quien lo lea.
    
    Las funciones se pueden documentar con \textit{docstrings}, explicando su utilidad y forma de uso inmediatamente después de definirlas:
\begin{minted}{python3}
def ejemplo(arg1, arg2=valor):
    """Descripción de lo que hace la
    función
    
    Entradas: Se describen sus entradas 
    (incluyendo tipo de dato)
    Salidas: Se describen sus salidas 
    (incluyendo tipo de dato)
    """
    operaciones
    return resultado
\end{minted}

    Las docstrings son un tipo de string multilínea, pueden escribirse con triple comilla simple (\mintinline{python3}|'''así'''|) o triple comilla doble (\mintinline{python3}|"""así"""|).

    La documentación de una función se puede ver con \mintinline{python3}|help| en la consola:
\begin{minted}{python3}
help(ejemplo)
\end{minted}
    
    \textbf{N.B.}: Los comentarios son para quien leerá o modificará el código, las docstrings son para quien utilizará la función en el suyo propio.
\end{contentbox}

\begin{contentbox}{label=Importación de Módulos Propios}
    Si escribimos funciones y constantes en un archivo independiente, puede importarse como cualquier otro módulo:
\begin{minted}{python3}
# Si utilidades.py existe en la
# misma carpeta
import utilidades
\end{minted}

Alternativamente,
\begin{minted}{python3}
from utilidades import func
\end{minted}
    
    Se pueden importar nombres de funciones y constantes.
    
    El nombre del módulo no puede contener espacios.
\end{contentbox}
